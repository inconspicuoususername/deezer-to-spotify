[project]
name = "deezer-to-spotify"
version = "0.1.0"
description = "Playlist migration thing"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
    "spotipy>=2.26.0",
    "tqdm>=4.69.1",
]
